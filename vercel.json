{
  "buildCommand": "npm run build && npm run build:api",
  "outputDirectory": "dist",

  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
